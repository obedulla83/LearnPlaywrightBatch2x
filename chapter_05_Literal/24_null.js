let no_audi_pramod_sir_has = null; // this is a null literal assigned to a variable no_audi_pramod_sir_has
let promod_has_two_wives; // this is an undefined literal assigned to a variable promod_has_two_wives

let age = 0;

let audi = "";
console.log(typeof audi);
console.log(typeof age);